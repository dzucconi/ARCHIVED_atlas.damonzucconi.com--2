{
  "name": "atlas.damonzucconi.com",
  "scripts": {
    "clean": "rm -rf ./dist",
    "copy": "cp -r ./public/* ./dist",
    "compile": "parcel build src/index.html",
    "build": "yarn clean && yarn compile && yarn copy",
    "start": "parcel src/index.html --open"
  },
  "dependencies": {
    "@types/page": "1.11.3",
    "map-cursor-to-max": "1.0.0",
    "page": "1.11.6",
    "qs": "6.10.1"
  },
  "devDependencies": {
    "@types/qs": "6.9.7",
    "parcel": "1.12.4",
    "sass": "1.37.1",
    "typescript": "4.3.5"
  }
}
